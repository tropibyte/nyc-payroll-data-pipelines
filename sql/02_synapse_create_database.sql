/* =====================================================================
   Run against: Synapse Analytics workspace > Develop > SQL script
   Connect to: Built-in (serverless SQL pool),  Use database: master

   You cannot run DDL in the default 'master' database, so create one
   first, then REFRESH the database dropdown and select 'udacity'
   before running 03_synapse_external_table.sql.
   ===================================================================== */
CREATE DATABASE udacity;
GO
